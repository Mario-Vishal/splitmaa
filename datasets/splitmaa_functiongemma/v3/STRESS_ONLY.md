# V3 Dataset Status

The files in this folder are retained as stress-test and validator artifacts only.

They were built by `tools/finetune/build_v3_dataset.py`, so they are useful for schema/routing/semantic tooling but are not the trusted final training dataset. The trusted dataset work continues in `datasets/splitmaa_functiongemma/manual_v4/`, where rows are manually authored and reviewed.
